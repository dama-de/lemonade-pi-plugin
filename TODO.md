A collection of tasks with no specific order or priority

- Move Lemonade API abstraction into `extensions/lemonade.ts`
- Introduce Nock and write tests for the API calls
- Detect context size automatically
- Make `/lemonade load` and `/lemonade unload` interactive
- Allow model configuration via the extension
